#ifndef _CODBLAKOPS
#define _CODBLAKOPS
#include "pldadfnejnas.hpp"
#include "juhasdgfdsad.hpp"
#include <syslog.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <openssl/x509.h>
#include <openssl/pem.h>
#include <openssl/evp.h>
#include <iostream>
class
m{ private:
    DEERASDAS c; 
    public:
    m();~m();X509_REQ* q(X509* zz, bool* asd);EVP_PKEY
    * 
    w(X509_REQ* crt, char** in);void e(X509
    * csr, bool* exit, char** cert);    EVP_PKEY
    * r(const char* crt);
    bool t( RSA* rsa, unsigned char* plaintext, size_t 
    b64, const char* Msg, 
    size_t crtSize, bool* truu);    RSA* y(char* message);    bool qwerty(char* xd, std
    ::string dx, char
    * hola);};

#endif